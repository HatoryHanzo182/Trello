@{
    ViewData["Title"] = "Board";
}
@model Trello.Models.Task

<div class="head">My Boards</div>
<div class="main-table">
    <div class="tack">
        <div class="task-title">
            <!--@Model.TaskTitle-->TO DO
            <button id="add_task_item" class="add-task-item-btn"><img src="~/img/add_FILL0.svg" alt="Add Task item"/></button>
        </div>
        
        @foreach (TaskItem i in Model.Items) { <partial name="TaskItem" model="@i"/> }
    
    </div>
</div>
<script src="~/js/HandlerModalWindows.js"></script>