@{
    ViewData["Title"] = "Board";
}
@model Trello.Models.Task

<div class="head">My Boards</div>
<div class="main-table">
    <div class="tack">
        <div class="task-title">@Model.TaskTitle</div>

        @foreach (TaskItem i in Model.Items)
        {
            
        }
    </div>
</div>